# 到期管家 for Scripting

紧凑原生风格的 iPhone 到期事项组件，支持信用卡账单、订阅、周期事项与 Apple 提醒事项。**3.0.0** 带来统一的 Icons8 Windows 11 Color 离线图库：829 枚应用、品牌及生活图标，23 个分类，同时保留 130 个 SF Symbols。每条事项独立选择，主界面及小、中、大组件同步显示；已有事项、周期、通知、完成记录与备份继续兼容。

周期可输入 1–99 的正整数，提前处理可设 0–365 天；两者在同一「重复」卡片，左侧始终有名称与单位。提前处理统一从提前日零点开始，未提前的定时事项按真实到期时刻处理，全天事项从到期日零点进入待处理且次日才逾期。月末及闰日锚点保持不变。只设置提前天数不会产生通知；要接收横幅，请另外开启「通知与提醒」。

## 版权与使用限制

Copyright (c) 2026 MaroonYS. All rights reserved. 本项目公开可读，但**不是开源许可项目**。完整条款见 [LICENSE](./LICENSE)，权利范围与第三方声明见 [NOTICE.md](./NOTICE.md)。

允许个人非商业安装、运行、更新、必要备份及分享官方链接。未经事先书面许可，禁止摘取再用、改名冒充、去除署名、转载、镜像、重新打包、二次分发及商业化；注明出处不等于获得授权。法定例外、GitHub 平台已有授权、第三方权利及有效的先前许可不受影响。声明仅限依法可受保护且有权授权的部分，不取得用户数据的权利。

公开脚本不能保证绝不被复制。安装包内附完整许可，主界面提供「版权与官方来源」；v2.5.9 起发行附带 SHA-256 校验文件和 GitHub 签名来源证明。不添加设备追踪、远程授权锁或清空数据机制。

核验方法、取证步骤及保护边界见 [保护说明](./docs/PROTECTION.md)。

## v3.0.0 · 统一彩色图库

- 采用 [Icons8 Windows 11 Color](https://icons8.com/icons/fluency) 原图：766 枚应用／品牌等图标及 63 枚财务、日期、家居和生活图标。包含产品系列与部分变体，不代表 829 个独立 App；未收录的应用继续使用 SF Symbols，不用旧混合图库补缺。
- 「图标与外观 → 图标图库与逐项设置」可预览和查找事项；编辑事项 →「图标」→「彩色图标」可搜索中英文名称、按 23 类筛选，每页 24 枚。生活图标使用中文名称；SF Symbols 的自动匹配和原有分类完整保留。
- 手动事项的选图与事项一起保存，返回取消不落盘；保存失败不关闭编辑页。Apple 提醒事项仅保存本地外观选择，不修改系统名称、备注、到期日或完成状态。每项独立，改一条不会切换全局模式或改其他事项。
- 主列表、小号主图标和下一事项预告、中大号列表及大号摘要共用同一选择；锁屏附件保持系统符号。彩色完成图标为 24 pt，主列表／小号／大号热区 40 pt，中号仍为 38 pt；文字详情、周期防重、只读与缓存保护保持不变。
- 829 张原始 96×96 PNG 及其逐图离线 JSON 随包附带，不裁剪、不抠图、不拉伸或重绘；29 枚深色标志仅在深色模式增加浅色底板。只解码当前页或可见组件行，独立回退、读取期限及有限缓存避免整库图片阻塞启动；无运行时图标网络请求。
- 旧混合品牌库及工具已移除；旧品牌选择只作兼容元数据保留，不自动映射到新图。已选 SF Symbols 不变，无需重建事项。新选择可随快照及 JSON 备份恢复；保留合法的未知图标 ID，当前无法显示时回退系统符号。
- 素材权利及逐图来源见 [Icons8 声明](./到期管家/assets/icons8/NOTICE.md) 与 [来源清单](./到期管家/assets/icons8/sources.json)。测试和真机待验收边界见 [3.0 QA 清单](./docs/QA-v3.0.0.md)。

2.8.1 是未独立发布的旧图库移除阶段，已并入 3.0；阶段记录保留于 [历史 QA](./docs/QA-v2.8.1.md)。

## v2.8.0 的独立图标选择与完整图案（历史）

- 编辑事项 →「选择图标」顶部并列提供「系统图标／品牌 Logo」。每条事项独立选择，保存事项后生效；取消或保存失败不留下新选择，选择品牌也不清空原系统符号。
- 「显示与组件 → 事项图标」可逐项设置手动事项及 Apple 提醒事项。去掉全局品牌模式，改一条不影响其他事项；主界面与小号组件遵循同一选择。中大号、底部预告和锁屏仍使用系统图标。
- 已明确指定的品牌保留；未指定品牌或选择系统图标时，只使用系统符号，不再因标题含品牌名称而自动切换。旧全局字段仅兼容保留在备份中，不再决定显示。
- 保留完整图案及原背景，只去掉打包新增的外圈留白；常规图案等比放入 24 pt 内容边界，SafePal 保持 20 pt。取消圆形遮罩，避免 Ultra Mobile 等方形图案被切角或出现外圈白色残边；不批量抠图、不拉伸或重绘素材。不同原图内部留白仍可能带来视觉大小差异。
- 40 pt 完成热区、事项／周期校验、只读与缓存保护、轻量启动及图片读取回退保留。PNG、离线 JSON、来源哈希和存储格式不变，无需重建事项。
- 316 项自动化测试在三个时区通过，全库本机深浅色参考图已核对；iPhone 真机排版、交互与着色仍需验收，详见 [QA 清单](./docs/QA-v2.8.0.md)。

以下为历史版本记录；3.0 已替换旧品牌图库，以下旧品牌选择、加载及裁剪规则不再适用于当前版本。

## v2.7.3 的圆形裁剪调整

- 330 个放大图案的圆形裁剪直径由 24 pt 调为 28 pt，相当于每侧多留 2 pt；图片本身仍为原来的 28.8 pt，不再次放大。
- 主界面、编辑预览、品牌目录和小号组件使用相同规则。保留圆形外轮廓和适量底板留白；SafePal、Telegram 未放大的原图保持 20／24 pt。
- 完成按钮仍为 40 pt，选择品牌、自动匹配、事项完成和启动逻辑不变。PNG、离线备份、图片来源和用户数据均未修改。
- 309 项自动化测试在三个时区通过，全部品牌的本机深浅色参考图已核对；不等同于 iPhone 真机验收，详见 [QA 清单](./docs/QA-v2.7.3.md)。

## v2.7.2 的启动兼容修正

- 启动入口仅依赖基础原生控件，先展示阶段信息，再加载主界面代码和检查数据。异步启动超过 10 秒显示可重试错误；关闭界面不等待后台通知维护。
- 删除约 4.17 MB 的整库 Base64 代码模块，改为 333 个独立 JSON 图片备份。PNG、图片字节和来源哈希不变；安装包仍包含全库，不代表安装体积缩小同样大小。
- 主界面和编辑页先显示可用的系统图标，仅在行出现后异步读取所需图片；目录仍按 32 项分页。单图读取超过 1.5 秒回退，迟到结果不会覆盖已关闭或已切换的行。
- 小号组件只预读当前选中品牌；系统模式、中大号和锁屏不读取图片。小组件专用着色属性仅留在小组件中。
- 事项、品牌选择、完成记录、周期规则与存储格式不变。旧版打不开时直接用下方安装链接更新，不需要先运行旧脚本，也不要删除它。
- 本地自动化通过不等于已在用户 iPhone 复现并确认解决。宿主原生死锁或脚本执行前的导入故障不一定能由启动页捕获；验证范围见 [QA 清单](./docs/QA-v2.7.2.md)。

## v2.7.1 的图标显示与点击修复

- 主界面列表读取已保存的品牌选择，修复招商银行等已选品牌仍显示系统图标。明确选定的品牌在主界面显示，不受小组件系统模式影响；自动识别仍遵循模式和手选系统图标的优先规则。
- 主界面左图标改为独立的 40 pt 完成按钮，右侧文字继续进入编辑页。校验当前事项及周期，防止快速重复点按和旧列表误推进；完成后刷新失败只提示警告，不误报完成失败。已完成或隐藏项不提供完成按钮。
- 小号组件的品牌图片放入完成按钮的实际标签内，替代透明系统图标加按钮背景的组合；采用文档支持的矩形点击形状，保留原事项／周期参数和只读、缓存保护。
- 主界面、编辑预览、品牌目录和小号组件共用圆形显示，裁去 330 个打包图片新增的外圈留白。图片原文件与来源清单不变；仅图片裁成圆形，40 pt 完成热区不缩小。SafePal 保留 20 pt，Telegram 保留 24 pt，不额外放大原图。
- 299 项回归测试在三个时区通过；图片参考图已核对，iPhone Scripting／WidgetKit 真实点击、着色及 VoiceOver 仍需验收，详见 [验证说明](./docs/QA-v2.7.1.md)。无需重建事项或重新选择已保存的品牌。

## v2.7.0 的完整品牌图标库

- 主界面「显示与组件 → 小组件图标」选择「系统图标」或「品牌 Logo 优先」。升级后默认保持系统样式，设置可随本机快照及 JSON 备份恢复。
- **332 个品牌全部内置实际图片**，含银行、支付卡、36 个运营商、汽车与多类订阅；不再只有名称。可搜索、按分类筛选，每页最多加载 32 个图标。
- 编辑事项 →「选择图标」→「图标类别」切换系统图标／品牌 Logo。品牌选择先暂存，保存事项时一并保存并开启品牌优先；关闭编辑页或保存失败不会留下品牌设置。原「小组件图标」入口继续用于全局模式与 Apple 提醒事项的逐项选择。
- 图片随包内置，附带同字节 Base64 离线备份以应对导入后素材路径变化；明确区分未收录、宿主不支持解码和加载失败，不再统一显示“无素材”。
- 自动识别仅使用本机事项标题，遇到多品牌、含糊短词、缺图或解码失败均回退。已手选的 SF Symbol 优先保留，除非再明确指定品牌；不读取 SIM、联系人或上传事项。
- 只替换小号组件当前事项的主图标，保留 40 pt 完成热区、原始事项和周期标识及只读／缓存保护。标题详情跳转、底部预告、中大号和锁屏组件保持原操作与图标。
- 图片来源包括品牌网站、对应官方 App 图标、Simple Icons 和 LobeHub，逐项记录来源、处理和 SHA-256。新增图片等比转换为 PNG 并加中性底板留白；SafePal、Telegram 原文件保持不变。第三方图片不属于本项目独占版权，收录不代表品牌授权或背书。详情见 [素材来源](https://github.com/MaroonYS/scripting-due-manager/blob/v2.8.0/到期管家/assets/brands/SOURCES.md) 和 [验证说明](./docs/QA-v2.7.0.md)。WidgetKit 实际颜色、点击和 VoiceOver 仍需 iPhone 验证。

## v2.5.11 的图标调整

- 「在组件显示金额」使用线框纸币图标 `banknote`，不再使用实心版本。
- 保持 24 pt 图标槽、20 pt 图标字号、系统蓝色及 12 pt 图文间距；其他设置图标、桌面组件、数据与操作不变。

## v2.5.10 的界面优化

- 移除主界面重复的“版本 + 版本号”整行，保留「检查并更新版本」入口；更新页继续显示当前版本、最新版本及更新说明，版权页的发行版本也保留。
- 「在组件显示金额」改用更清晰的实心纸币符号；「显示 Apple 提醒事项」改用任务夹板；「提醒事项列表」改用圆形筛选符号。
- 三个预览入口改为带内容线条的微型组件：小号正方形、中号横向、大号纵向，统一圆角和描边，不再仅显示空心框。
- 六个图标使用相同的 24 pt 图标槽与 12 pt 图文间距，保持系统蓝色，不限制文字动态字号和换行。
- 仅调整设置主界面。开关、列表选择、三种预览操作、桌面组件内容与排版、事项数据及版权和签名校验保持不变。

## v2.5.9 的保护措施

- 仓库与安装包包含一致的 LICENSE、NOTICE.md；源码与测试带有版权和限制性许可标识，测试及发布校验会检测缺失声明。
- 新增主界面版权页，链接到当前安装版本的完整许可，保留官方仓库与发行入口；不改变任何尺寸的小组件排版。
- 官方流水线固定 Action 提交，验证源码／安装包一致性，生成 SHA256SUMS 与签名来源证明，再核验官方仓库、工作流、main 引用及提交后发布。
- 原固定安装／更新链接不变；不修改事项、完成记录、同步、通知、存储格式或现有数据。手机显示声明不代表本机文件已经验签。

## v2.5.8 的修复

- 组件同步“点按重试”打开主界面并重新同步；完成操作警告继续进入操作详情。
- 同步乱序返回时保留较新的缓存；完成事项或关闭同步后，在途的旧读取会被标记作废。同一列表的多款组件正常并发不会仅因重叠加载而出现错误提示。
- 等待 Apple 提醒事项后，组件读取最新手动事项和显示设置。若提醒列表变化，最多重试一次，避免旧列表内容与新设置混用。
- 离线缓存按当前日期范围过滤，并正确处理边界日的全天事项；不同查询范围不会互相冒充完整结果。
- 列表保存期间锁定操作，快速连续选择使用最新状态；过时的授权或同步回调不再覆盖新操作状态。
- 写入前拒绝无效日期与超出支持年份的周期推进，保留原数据和完成记录；年份 0001–0099 的日期计算和显示不再偏移到 1900 年代。

## v2.5.7 的修复

- `Wallet Plan` 不再是非信用卡事项也无法越过的 card 默认值；它现在是通用容器，未识别项使用中性任务图标。
- `Wallet Plan` 内支持严格的“模块名 + 1–3 位编号”前缀：`BANK 06` 显示银行，`CREDIT 09` 显示信用卡，并扩展到 `POINTS` / `INVEST` / `LOAN` / `INSURANCE` / `TAX` / `BILL` / `PAYMENT` 及简繁中文表达。
- 编号前缀高于后续品牌词，因此 `BANK 08 | Quicksilver` 仍是银行模块，`CREDIT 08 | SoFi` 仍是信用卡模块。同时要求标题开头且必须带数字，不会把 `bank holiday`、`bankruptcy`、`credit union` 或 `course credit` 误判为模块。
- 新增 Equifax、Experian、TransUnion、myFICO、Credit Karma 及信用报告／评分／监控语义，使用文档查询图标而不是信用卡。高置信度备注也可以覆盖 `Wallet Plan` 的中性后备。
- Apple/Scripting 公开的 Reminder 对象不提供 List 内 Section 名称，所以本版不伪造分区数据；分区语义需要出现在事项标题或备注中才能可靠区分。

## v2.5.6 的改进

- 小、中、大组件中的 Apple 提醒事项现在区分两种点击操作：左侧类别图标只负责完成，标题、备注／日期等内容区域则打开系统「提醒事项」中的对应事项；小号顶部日期／类别与大号顶部摘要也指向当前事项。
- 小号组件底部的下一事项预告也会打开预告所对应的 Apple 提醒事项，不会错误跳到当前事项；手动事项仍打开到期管家编辑页。
- 裸 UUID 和可确认的旧 URL 包装会统一生成 `x-apple-reminderkit://REMCDReminder/{UUID}`；本实现不附加在不同 iOS 版本上表现不一致的 `/details`。无法提取有效 UUID 时安全回退到通用 `x-apple-reminderkit://`，只打开系统「提醒事项」。
- 该跳转使用 Apple 未公开的 URL Scheme，不能承诺未来 iOS 版本永久兼容，发布前及系统升级后必须在 iPhone 真机验证。完成操作仍使用原有交互式按钮，不依赖这一 URL。

## v2.5.5 的修复

- 提醒标题中的明确品牌或具体事项始终拥有最高优先级；具体 List 分类以及 List 名中的品牌或产品信息也始终优先于备注。
- 备注中的品牌、产品或明确类别属于高置信度提示，可覆盖 `Tasks`、`Inbox`、`Personal`、`Reminders` 等通用 List；普通动作提示只在 List 完全没有命中时使用，避免含糊备注改掉可靠的 List 图标。
- 标题、List 与备注分别匹配，不会把分散在多个字段中的词拼接成不存在的品牌或类别。
- 提醒事项缓存快照只保存白名单内的图标提示及 `strong`／`ordinary` 置信度，不保存 Apple 提醒事项的备注正文、匹配词或文本片段。旧快照继续兼容；更新后运行一次主脚本完成实时同步，才能为旧提示补充置信度。
- 不改变小、中、大或锁屏组件的尺寸、排版、语言判断、点击行为和通知安排。

## v2.5.4 的修复

- `Card Setup`、`CARD—SETUP`、`VentureOne Bonus`、`Venture One Bonus` 及对应简繁体信用卡操作会从提醒标题识别为橙色信用卡图标。
- 完整 List 名 `Wallet Plan` 曾作为信用卡分类后备；该策略已在 2.5.7 改为混合模块识别。
- 不使用宽泛的单独 `card`、`wallet` 或“卡”匹配，并增加 `Cardinal`、`Venture Capital`、`Wallet Planning Workshop` 等负例保护。
- 继续保持标题、List 与备注分字段匹配，旧事项无需删除、重建或重新导入。

## v2.5.3 的修复

- 所有小、中、大及锁屏附件组件优先读取 `Device.systemLanguageTag`／`systemLanguageCode`，只有旧版 Scripting 未提供这些值时才退回 `Device.preferredLanguages`。
- 左上角图标语义名称、日期、内置标题、分区、空状态和错误提示共用这一语言来源。英文系统下，截图中的“税务会计”会显示为 “Tax & Accounting”。
- 明确选择简体或繁体中文时，脚本设置优先于地区推断；真实标题恰好为 “Untitled Reminder” 或“未命名提醒”时也会原样保留。
- 不迁移或重写任何事项，不改变组件尺寸、字号、行高、点击区域或通知安排。

## v2.5.1 的修复

- 通知按稳定事项身份匹配：文案变化逐条替换，未轮到的有效提醒继续保留；实际时刻变化优先修复已有排程。等待系统查询后及每次提交前重新校验当前时间，不把已过时间报告为未来待发。
- 主数据损坏时进入独立恢复页，不先启动普通主界面；恢复前隔离保存原始数据，保全失败不覆盖。未知更高格式禁止覆盖，可导出原始恢复档案。
- 恢复后，仅仍与完成记录精确匹配的事项保留安全撤销能力；异常大版本数字被拒绝，旧重复长 ID、关联通知开关及空白提醒标题能往返备份。
- Apple 提醒已完成后的缓存读写失败仅作为警告；缓存日期按当前时区更新。点「检查操作」直达结果说明页，也可在主界面进入「上次组件操作」。清除只作用于所查看的事件，不改变事项。

## 安全与提醒

- 「记录与数据安全」保留最近 100 条完成记录和 10 份本机快照。手动完成可安全撤销；后续已编辑、再次完成或删除的事项不会被覆盖。Apple 提醒事项须在系统 App 恢复。
- JSON 导出／导入包含手动事项、完成记录、显示设置和通知偏好；严格校验并在恢复前保存快照。恢复后通知先关闭，请核对后重新开启。超过 1,000 条事项不再被静默截断。
- 本地通知默认关闭，仅处理手动事项，可设提醒时间、到期日再提醒和单项关闭。滚动安排未来 90 天、最多 40 个提醒计划；组件普通新增或文案更新每次最多 3 条，已有提醒的实际时刻修复最多 40 条。逐条替换先确认新请求，再移除旧请求，瞬间可能保留 1 个替换副本；宿主总请求保守限制为 60。无替换余量或系统接口失败时保留旧请求并显示未完成状态，请打开主脚本重试。不保证无限期后台运行或准点横幅。
- 同步失败时显示最后成功读取时间及重试指引。更新页显示当前／最新版本和更新内容；手动安装前创建快照，并使用已核验的版本化资源，拒绝重复安装和降级。
- 本机快照仍在同一设备内，不能代替独立备份；导出的文件包含名称、金额和备注，请保存在可信位置。

详细更新内容见 [发行说明](./RELEASE_NOTES.md)，测试与真机待验收范围见 [QA 清单](./docs/QA-v3.0.0.md)。自动化测试不等于真机跳转、排版、动画或通知送达验证。

以下布局参数中的图标字号指 SF Symbols 分支。3.0 的彩色完成图标为 24 pt，小号预告 12 pt、大号摘要 30 pt；仅改变图片内容，原有热区、行高、容量及详情入口不变。彩色只读／缓存图标降低透明度，右侧的锁定／缓存状态保留。

现有组件排版保持不变：小号金额与备注继续左对齐；中、大号的金额和备注继续使用固定 116/124 pt 右栏与单行槽位，过长时先使用系统字距收紧，最低保持 85% 字号，再从中间省略以同时保留首尾信息。该策略不会改变标题宽度、组件容量、行高或完成图标点击区。所有组件文案优先读取 iPhone 系统语言，在简体中文、繁体中文和英文之间自动切换，其他语言使用英文兜底；日期始终按系统语言标记的完整地区格式显示。小号左上角仅保留当前图标的本地化语义名称，不再重复显示图标；当前事项图标仍是 17 pt 完成按钮，点击热区保持 40 pt，主体标题继续按实际宽度使用 16/15/14 pt 适配一、二、三行。小、中、大组件不显示“明天”“2 天后”“逾期 6 天”或 24 小时实时倒计时，只显示明确的本地化日期；小号底部预告同步显示下一事项日期。中号保持 17 pt 图标、38 pt 点击热区、2–3 项动态容量及无分隔线布局；大号保持 18 pt 行图标、40 pt 点击热区、26 pt 摘要图标，并按实际高度显示 3–5 项。中、大号标题最多两行。左侧事项图标只用于完成；手动事项的文字内容打开到期管家编辑页，Apple 提醒事项的标题、备注／日期以及小号下一事项预告会尝试直达系统对应提醒；小号顶部日期／类别和大号顶部摘要也指向当前事项。只读或缓存事项的图标会变灰，右侧保留绝对日期及锁定或缓存标识。大号继续使用系统「提醒事项」风格的摘要、分区和留白节奏，完成后由后续事项补位。交互后的内容更新时间仍由 WidgetKit 决定，不声称使用 Apple 私有的点击瞬时动画。Apple 提醒事项的详情跳转使用未公开的 `x-apple-reminderkit` URL Scheme；无法提取有效 UUID 时回退到通用 `x-apple-reminderkit://` 并打开系统「提醒事项」，且此能力需要真机持续验证。锁屏附件只显示总数，没有单条事项链接，继续打开 Scripting。Apple 提醒事项可按一个或多个具体列表筛选；图标离线分层推断：事项标题始终优先，随后是具体 List 分类及 List 名中的品牌或产品，高置信度备注可覆盖通用 List，普通动作备注只在 List 未命中时使用，最后才回退默认值。规则覆盖 126 个 List 图标方向与 1,100+ 个简体、繁体和英文别名；缓存只写入白名单图标提示及 `strong`／`ordinary` 置信度，不写入备注正文。手动事项提供 11 种到期类型，并内置 130 个本机 SF Symbols。

## 一键安装或更新

[在 iPhone 上安装或更新到期管家](https://www.scripting.fun/import_scripts/?urls=%5B%22https%3A%2F%2Fgithub.com%2FMaroonYS%2Fscripting-due-manager%2Freleases%2Flatest%2Fdownload%2Fdue-manager.scripting%22%5D)

固定链接始终指向 GitHub 最新发行版。安装后，脚本还会通过 `remoteResource` 使用同一地址检查更新；主脚本底部的「检查并更新版本」会先检查 GitHub 的正式版本和安装包，再显示更新说明，安装前保存快照并生成新的下载请求。发布流水线逐文件校验安装包与源码，不允许以不同内容覆盖已有版本。

从 1.2.0 升级时先不要删除手机里的旧脚本；让现有脚本更新到最新版后运行一次，它会把旧事项迁移到本机持久域。2.4.0 可兼容读取 schema v1/v2 数据，旧事项的提前天数默认为 0，并在下次保存时写为 schema v3；无需重新添加事项或桌面组件。

完整功能说明和安装要求见 [`到期管家/README.md`](./到期管家/README.md)。

## 仓库内容

- `到期管家/`：可维护的 Scripting 源码。
- `到期管家.scripting`：可直接导入的发行包。
- `tests/`：日期、周期、图标匹配、提醒事项分层图标识别、组件布局、队列补位、幂等完成、列表筛选、只读权限和冲突保护测试。
