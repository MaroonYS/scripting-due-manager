# 到期管家

一款为 [Scripting](https://apps.apple.com/us/app/scripting/id6479691128) 编写的 iPhone 桌面组件，用来统一跟踪信用卡账单、订阅、周期账单、一次性截止日期和 Apple 提醒事项。

## 固定安装与更新链接

[在 iPhone 上安装或更新到期管家](https://www.scripting.fun/import_scripts/?urls=%5B%22https%3A%2F%2Fgithub.com%2FMaroonYS%2Fscripting-due-manager%2Freleases%2Flatest%2Fdownload%2Fdue-manager.scripting%22%5D)

这个链接始终指向 GitHub 最新发行版的不可变安装包，不再直接依赖分支原始文件的 CDN 缓存。脚本也配置了 Scripting 官方的远程资源更新地址，每小时允许检查一次新版本；实际检查和安装时机由 Scripting 决定。首次安装仍需在 iPhone 上确认。

## 已实现

- 小、中、大组件按 Apple 公布的 iPhone WidgetKit 尺寸和实际 `Widget.displaySize` 分别排版；三种尺寸使用一致的视觉安全边距基准，其中中、大号组件为 14 pt 横向、11 pt 纵向内容边距，并使用系统语义色和完整的系统容器背景，不再出现内嵌黑色矩形。
- 小号组件优先展示当前最紧急事项，右上角显示该事项的到期日期；完成圆圈与当前标题共用固定纵向锚点，并整体上移 7 pt，使三行标题上下留白更均衡，同时不会带动备注和预告栏。16 pt 主标题的第一行与圆圈视觉居中，标题最多显示三行。原最下方的当前日期与状态栏已移除；13 pt 金额或备注固定显示在原预告位置，下一事项预告则固定作为最下栏。有无备注或下一事项时都保留槽位，各栏不会跳动。小组件内容左右各内收 3 pt，形成与相邻系统小组件一致的左基准线和对称安全边距；预告上方不绘制分隔线。
- 组件左侧提供类似「提醒事项」的完成圆圈；圆圈扩大了透明可点击区域并负责完成，图标与文字区域负责打开详情，避免嵌套点击冲突。
- 完成控件使用透明的交互式 `Button + AppIntent`；只挂载一层可点击内容，避免透明的旧控件遮挡新控件。完成成功后递增过渡代次（generation），由 WidgetKit 在新时间线到达时驱动当前内容淡出和下一件补位；已完成事项不会为制造动画而重新塞回显示队列。
- 手动事项提供 11 种到期类型：信用卡、订阅会员、账单缴费、贷款分期、保险保单、数字服务、证件合同、保养维护、预约日程、纪念日期和其他事项。类型菜单、存储校验、默认图标、颜色和同时到期的排序均由同一份定义生成。
- 内置 130 个本机 SF Symbols，并按财务、影音娱乐、工作效率、数字服务、社交通讯、居家生活、健康运动、出行旅行、学习阅读、创作设计和其他 11 个 App Store 常见方向分组；编辑事项时可手动覆盖，升级后仍兼容已经保存的旧图标。
- 根据名称离线匹配更多订阅品牌与类别，包括 AI、影音、音乐、游戏、新闻阅读、设计创作、办公效率、云服务、安全、购物、餐饮、健康、教育和出行等方向。匹配时优先采用较长、较具体的短语，并收窄容易误判的通用关键词。
- 导入的 Apple 提醒事项采用分层图标识别：先匹配事项标题，再参考所属 List 名称，然后查看备注，最后才使用默认提醒图标。常见购物、快递、会议、家务、医疗、学习和出行等动作，以及账单、订阅、工作、家庭、健康等 List 类别均可自动匹配；标题中的明确品牌或具体事项始终优先于 List 的宽泛类别。
- 图标匹配完全离线，不查询 App Store、品牌图标库或其他网络服务，事项名称、List 与备注不会因匹配图标而上传。缓存只保存推断出的图标提示，不保存 Apple 提醒事项的备注正文。
- 小号组件显示最紧急的 1 件；中号按高度显示 2–3 件；大号按高度显示 5–7 件。
- 完成当前事项后，新出现的下一件可立即继续点击；旧时间线中的控件仍会由代次校验拒绝，避免重复完成同一期事项。
- 后续事项在固定槽位中自动补入；同一天有具体时间的事项按时间先后排列。
- 完成按钮带有本期识别码，重复点击或旧组件快照不会把周期事项误推进两期。
- 手动周期事项完成一次只推进一期；一次性事项完成后移入「已完成或隐藏」。
- Apple 提醒事项可直接从组件标记完成；系统周期提醒的下一次实例由 iOS 生成。只读列表中的事项会显示锁图标且不能从组件完成。
- Apple 提醒事项默认读取全部列表，也可在主脚本中按一个或多个具体列表筛选；同名列表会显示所属账户以便区分。若所选筛选范围已失效或暂时无法读取，原有筛选设置不会被错误覆盖。
- Apple 提醒事项在小号组件中将所属列表名称显示在中间备注栏；手动事项的金额和备注使用同一位置。
- 提醒事项读取失败时最多回退到 24 小时内、且筛选范围完全相同的成功快照；缓存行会明确标记且不可从组件完成。实时读取已经成功但快照写入失败时，会单独提示缓存保存失败，不会把实时数据误报成读取失败。提醒事项快照继续使用 schema v1，新版新增的派生图标提示为兼容字段，旧快照可直接读取并在缺少提示时重新推断。
- 支持一次性、每 N 天、每 N 周、每 N 月和每 N 年，以及月末和闰日规则。
- 在 Scripting 内直接新增、编辑、删除和完成事项，无需修改代码；若旧编辑页打开后同一事项已在别处更新，保存或删除会提示冲突，避免旧内容覆盖新数据。
- 事项保存在 Scripting 的本机共享 `Storage` 域，不会被打进远程安装包；首次运行 1.2.1 或更高版本会自动迁移旧版私有数据，后续替换脚本包无需重新录入。1.3.26 可无损读取旧的 v1 状态，下次保存时写为 v2；旧脚本会拒绝覆盖 v2 数据，避免降级时把新类型误改为「其他事项」。
- 保存后会等待存储落盘，再优先刷新用户小组件，避免必须删除并重新添加组件才能看到新数据。

## 安装要求

- iOS 17 或更高版本（交互式桌面组件要求）。
- Scripting 2.4.9 或更高版本（直接完成 Apple 提醒事项使用 `Reminder.get`）。

安装步骤：

1. 在 iPhone 上打开上方固定安装链接，确认更新或导入「到期管家」。从 1.2.0 升级时不要先删除已有脚本。
2. 更新后运行一次主脚本；1.2.1 或更高版本会自动迁移已有事项并刷新用户小组件。全新安装时再添加手动事项。
3. 如需合并 Apple 提醒事项，打开对应开关并完成授权。随后可进入「提醒事项列表」选择全部列表或一个/多个具体列表。首次打开列表选择页会申请日历与提醒事项权限；权限必须在可见的主脚本内授予，桌面组件无法弹出授权框。
4. 长按 iPhone 主屏幕，添加 Scripting 组件。
5. 长按新组件 →「编辑小组件」→ 选择「到期管家」。
6. 分别在真实主屏幕上检查你使用的小、中或大号版式。

以后更新时可继续使用同一个链接；启用远程资源后，Scripting 也会按自己的调度检查固定地址。无需保存不同版本的 `.scripting` 文件。

## 使用方式

- 点事项左侧空心圆圈可完成当前一期；点文字可打开事项详情。
- 新事项默认使用「自动匹配」图标；在编辑页点「图标」可固定为任意内置语义图标，再改名称也不会被自动替换。
- 对手动周期事项，组件完成一次只推进一个周期，不会静默跳过累计逾期记录。
- 对 Apple 提醒事项，组件会直接把系统提醒标记为完成；若它是重复提醒，下一次出现时间由 iOS 控制。
- 完成按钮只记录「本期已处理」。它不会自动付款、续订、取消订阅或操作银行账户。
- 设备锁定时，iOS 可能要求先解锁才能执行交互。
- 可在主脚本中预览三种尺寸，并手动请求刷新桌面组件。

## iOS 刷新限制

桌面组件不是常驻进程。脚本会在下一个本地午夜、最近的具体到期时刻、开启系统提醒事项后的约每 3 小时，或用户交互时请求新数据，但最终执行时间由 WidgetKit 决定，可能延后。

动态 `DateLabel` 能在脚本未运行时继续更新倒计时文字，但不会重新查询 Reminders。如需必须准时弹出的通知，应同时在 Apple 提醒事项或日历中设置通知。

Apple 的原生 WidgetKit 支持自定义 `ToggleStyle`，但 Scripting 当前只开放 `automatic`、`switch` 和 `button` 三种内置 Toggle 样式，也没有把乐观更新后的 `isOn` 暴露给自定义 TSX 子视图。内置 button Toggle 会强制绘制无法单独缩小的灰色底盘，所以本脚本改用透明的 plain Button，优先保证外观和连续点击；完成后以 generation 驱动 WidgetKit 的内容过渡，圆环及队列仍只能在系统交付新时间线后变化。刷新调度与即时反馈由 WidgetKit 控制，因此这不是、也不会冒充 Apple「提醒事项」使用私有能力实现的点击瞬间动画。要完整复刻该效果，需要原生 SwiftUI 小组件或等待 Scripting 开放自定义 `ToggleStyle`。

手动事项的具体时间按设备本地时钟保存；跨时区旅行时会保持相同墙钟时间。系统提醒事项的具体时间则按其绝对时刻换算为设备当前时区。

共享 `Storage` 只保存在本机，但不是密码保险箱，同一 Scripting 中的其他脚本理论上也能访问共享域。请勿填写完整卡号、密码等秘密；事项标题和金额也可能直接显示在锁屏可见的主屏幕组件上。

## 源码结构

```text
到期管家/
├── script.json
├── index.tsx
├── widget.tsx
├── app_intents.tsx
└── src/
    ├── date.ts
    ├── icons.ts
    ├── item_kinds.ts
    ├── presentation.ts
    ├── reminders.ts
    ├── storage.ts
    ├── types.ts
    ├── widget_completion.ts
    ├── widget_layout.ts
    ├── widget_refresh.ts
    └── widget_view.tsx
```

`.scripting` 是 ZIP 容器，上述文件位于压缩包根目录，不应再多包一层同名文件夹。
