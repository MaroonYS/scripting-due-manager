# 到期管家 v2.5.1

## 本次修复

- 通知使用稳定事项身份，切换语言不再批量取消有效旧通知；逐条确认新请求后才移除对应旧请求。实际提醒时刻变化优先修复已有排程。
- 通知查询完成后、提交前和确认后重新核对当前时间，避免排入过去的时刻并误报未来待发。
- 主数据损坏时可进入独立恢复页，从有效 JSON 或本地快照恢复；写入前隔离保全原始数据，失败不覆盖。未知更高格式禁止覆盖，支持原始恢复档案导出。
- 恢复备份后，只对与历史完成状态精确匹配的事项恢复撤销能力，仍阻止覆盖之后的编辑或再次完成。
- 修复旧重复长 ID、关联通知开关和空白 Apple 提醒历史的备份兼容；严格限制时间戳／版本数值，避免旧按钮失效保护被异常大数字破坏。
- Apple 提醒完成后的缓存读取、迁移和写入失败统一降级为“已完成但缓存异常”，不再误报完成失败。
- 跨时区离线缓存重新计算定时事项的本地日期和时间；全天事项仍保持原日历日期。
- 组件「检查操作」直达真实状态页，可查看结果与处理建议；确认清除只移除所查看事件的提示，不会再次完成事项。同文案的新事件不会被旧页面清除。

## 更新与使用

现有小、中、大组件布局不变；小型备注左对齐，具体时间继续在备注行右侧。更新后运行一次脚本，无需重新添加事项或组件。

恢复后通知先关闭，请核对后重新开启。原始恢复档案用于保全和排查，不可当作普通备份直接导入；导出文件含事项和设置，请保存在可信位置。本机快照不是独立的异地备份。

## 明确限制

- Apple 提醒事项的完成会记录，但撤销应在系统 App 内进行；脚本不能可靠核验其后续修改，不做可能造成重复的自动反向操作。
- 通知安排未来 90 天、最多 40 个逻辑提醒计划。组件普通新增／文案更新每次最多 3 条；已有请求的时刻修复最多 40 条。先新增后撤旧的替换阶段可能短暂多 1 个副本，宿主总请求保守限制为 60；无替换余量或接口失败时明确显示未完成，请打开主脚本重试。
- 原生调用中断在新增与撤旧之间时可能留下副本，下次对账会去重。需要后续运行来持续补充，不能保证无限期后台执行。
- “排入通知”不代表系统一定立即显示横幅；通知权限、专注模式、摘要和系统调度仍会影响实际到达。
- 共享存储协调降低并发竞争，但没有跨进程原子事务保证；原生布局、VoiceOver、动画与真实通知送达仍需 iPhone 验收。

回归范围与真机验收步骤见 `docs/QA-v2.5.1.md`。
