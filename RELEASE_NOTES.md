# 到期管家 v2.5.4

## 本次修复

- 补齐 Apple 提醒事项中的信用卡标题识别：`Card Setup`、`CARD—SETUP`、`VentureOne Bonus`、`Venture One Bonus` 以及常见简繁体开卡、激活和自动还款表达现在使用橙色信用卡图标。
- 将完整 List 名 `Wallet Plan` 加入信用卡分类后备。具体标题仍然优先于 List，不会改变原有分层顺序。
- 保持严格边界：没有加入宽泛的单独 `card`、`wallet` 或“卡”，并验证 `Cardinal Setup`、`Venture Capital Review`、`Wallet Planning Workshop` 等相近无关文字不会误判。
- 提醒快照继续保存原始标题与 List，而不是最终图标；更新后旧缓存也会用新词库重新推断，无需重新添加提醒事项。

## 更新与使用

本次不迁移或重写事项，不改变小、中、大组件的尺寸、字号、间距、容量、点击区域、系统语言逻辑或通知安排。更新后运行一次主脚本并等待 WidgetKit 刷新即可。

## 明确限制

- 图标推断完全在本机按可审计词库完成，并非联网 AI；未覆盖的新品牌或高度含糊的标题仍会安全回退到 List 分类或任务图标。
- WidgetKit 决定桌面组件的实际刷新时机；短时间显示旧图标不表示标题或数据丢失。

回归范围与真机验收步骤见 `docs/QA-v2.5.4.md`。
