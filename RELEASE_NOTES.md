# 到期管家 v2.5.0

## 本次更新

- 小型组件右上角仍只显示日期；具体时间放在原有备注栏右侧。备注仍左对齐，标题和底部预告的位置不变。
- 提前处理统一从提前日零点开始，编辑页明确显示“开始处理”时间；未提前的定时事项按真正到期时刻处理。
- 新增“记录与数据安全”：最近 100 条完成记录、手动完成安全撤销、最近 10 份自动本地快照、JSON 文件导出与恢复。
- 修复超过 1,000 条手动事项被静默截断的问题；导入严格校验，恢复前自动备份，恢复后旧组件按钮失效，避免误完成。
- 新增默认关闭的本地通知：自定义提醒时刻、到期日再提醒、单项关闭、滚动排程、自动取消旧通知和去重。只处理手动事项，不与 Apple 提醒事项重复通知。
- 真正的版本检查页：显示当前／最新版本、更新内容和失败原因；安装前备份并锁定已核验的版本安装包，防止降级。
- 同步问题显示最后成功时间与重试指引；保存成功后的通知或组件刷新异常仅作为警告，不再误报保存失败。
- 完成图标保留系统语义名称和原有触控尺寸，便于辅助功能识别。

## 更新与使用

更新后无需重新添加事项或组件。主界面新增“通知与提醒”和“记录与数据安全”；通知默认关闭，需主动开启并允许 Scripting 通知权限。

恢复备份会恢复其中的通知时刻和单项设置，但先关闭通知；请检查后重新开启。备份包含事项名称、金额、备注和完成记录，请保存在可信位置。本机快照不是独立的异地备份。

## 明确限制

- Apple 提醒事项的完成会记录，但撤销应在系统 App 内进行；脚本不能可靠核验其后续修改，不做可能造成重复的自动反向操作。
- 通知滚动安排未来 90 天、最多 40 条，并为其他脚本保留预算；组件每次最多补充 3 条。需要后续运行来持续补充，不能保证无限期后台执行。
- “排入通知”不代表系统一定立即显示横幅；通知权限、专注模式、摘要和系统调度仍会影响实际到达。
- 共享存储协调降低并发竞争，但没有跨进程原子事务保证；原生布局、VoiceOver、动画与真实通知送达仍需 iPhone 验收。

已完成本地自动化回归和源码检查；真机验收清单见仓库 `docs/QA-v2.5.0.md`。
