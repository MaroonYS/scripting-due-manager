# 到期管家 v3.4.1

完善「点击小组件左上角主题查看对应提醒备注」。

- 主题、系统提醒标题／日期和下一项预告均携带各自准确的事项 ID，进入独立只读备注页；旧版组件的 open-reminder 参数同样兼容。
- 先显示完整备注、原换行、标题、所属列表及已完成状态；不再把跳出 Scripting、打开 Apple 提醒事项详情作为阅读备注的前提。
- 提供加载、空备注、已完成、删除／不可用、权限不足、10 秒超时和重新读取状态。快速切换或关闭页面时忽略迟到结果，防止显示其他事项的备注。
- 「在提醒事项中打开」为明确的次级操作，打开前再次核对 ID；失败仍可阅读备注，不自动打开正文中的链接。非 UUID 账户可读取备注，即使不能直接跳入系统详情。
- 左上角主题标签增加矩形点击区域和 24 pt 最小高度。主界面新增「系统提醒事项 → 查看提醒事项备注」，按现有范围查找，不在缺失参数时猜测目标。
- 备注只在页面打开时读取并保留于页面内存，不写入缓存、URL、备份或远程服务。事项完成、日期、周期、通知和 182 个系统图标保持不变。

更新原脚本后运行一次，并请求刷新桌面小组件；不需要删除脚本、事项或重新授权图库。

328 项测试在香港、UTC、纽约三个时区全部通过，严格类型检查通过。验证及真机边界见 [3.4.1 QA](https://github.com/MaroonYS/scripting-due-manager/blob/v3.4.1/docs/QA-v3.4.1.md)。用户报告 3.4.0 的点击只打开 Scripting；本次移除了读取备注所依赖的第二次外部跳转，并补充可见备注界面，但宿主在 iPhone 上的真实点击／传参与私有详情链接仍需设备确认。
