# 到期管家 v2.7.2

## 启动兼容修正

- 拆出轻量启动页：先显示当前阶段，再加载主界面代码和检查本机数据；异步启动超过 10 秒显示可重试错误。关闭界面不等待后台通知维护。
- 移除约 4.17 MB 的整库 Base64 代码模块，改为单张图片的独立离线 JSON 备份。保留全部 332 个品牌和 333 个原 PNG，图片字节及来源哈希不变。
- 主界面、编辑预览先显示可用的系统图标，只有可见行异步读取图片；目录保留 32 项分页。单图 1.5 秒超时回退，迟到结果不会覆盖已关闭或已切换的行。
- 小号组件只读取当前品牌；系统模式、中大号和锁屏不读图片。小组件专用着色属性不再用于主界面。
- 保留品牌选择、左图标独立完成、40 pt 热区、精确事项／周期校验与只读保护；不更改存储格式、周期规则、提醒同步或通知实现，不清空已有事项和设置。

旧版无法打开时，直接使用原官方一键安装链接更新，无需先进入旧脚本。不要删除旧脚本或重新建立事项。

308 项回归测试在三个时区通过，严格类型检查通过。当前没有用户 iPhone 的运行日志，也未在其设备复现：以上是针对已确认的代码风险作出的兼容修正，不宣称已证明唯一根因或保证真机恢复。执行脚本前的导入故障、原生阻塞可能无法由启动页捕获。完整验证范围见 docs/QA-v2.7.2.md。
