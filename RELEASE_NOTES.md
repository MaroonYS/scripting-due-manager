# 到期管家 v2.5.11

## 金额设置图标统一为线框式

- 仅将「在组件显示金额」的 `banknote.fill` 改为线框式 `banknote`，不使用实心图标。
- 保持 24 pt 图标槽、20 pt 图标字号、系统蓝色、12 pt 图文间距及原对齐。
- 其他设置图标、三种组件预览、桌面组件、事项数据、版权声明与更新校验均不变。

使用原固定安装链接或主脚本底部「检查并更新版本」升级，再运行一次主脚本即可。
无需重新添加事项或小组件。验证范围见 docs/QA-v2.5.11.md。
