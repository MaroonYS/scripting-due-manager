# 到期管家 v2.5.2

## 本次修复

- 修复显式 iPhone 系统语言与 Scripting 返回的首选语言不一致时，组件错误采用首选语言的问题。
- 所有小、中、大及锁屏附件组件现在优先读取 `Device.systemLanguageTag`／`systemLanguageCode`；旧版 Scripting 未提供这些值时才退回 `Device.preferredLanguages`、系统 Locale 与安全英文后备。
- 小号左上角图标语义名称、日期、内置标题、分区、空状态和错误提示共用同一系统语言来源。英文系统下，“税务会计”会显示为 “Tax & Accounting”，日期也使用英文系统格式。

## 更新与使用

本次不迁移或重写事项，不改变小、中、大组件的尺寸、字号、间距、行高、容量、点击区域或通知逻辑。更新后运行一次脚本，再等待系统刷新组件；如旧时间线仍在，可长按组件进入编辑后直接退出以请求刷新，无需删除组件或重新添加事项。

## 明确限制

- WidgetKit 决定桌面组件时间线的实际刷新时机，更新脚本后短时间保留旧文案不代表数据丢失。
- 其他未受此次修复影响的系统边界继续适用：通知实际送达受权限与系统调度影响；共享存储没有跨进程原子事务；原生动画、VoiceOver 与实际尺寸仍需 iPhone 验收。

回归范围与真机验收步骤见 `docs/QA-v2.5.2.md`。
