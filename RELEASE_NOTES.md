# 到期管家 v2.5.5

## 本次修复

- Apple 提醒事项标题中的明确品牌或具体事项始终拥有最高优先级；具体 List 分类以及 List 名中的品牌或产品信息也始终优先于备注。
- 备注中的品牌、产品或明确类别现在作为高置信度提示，可覆盖 `Tasks`、`Inbox`、`Personal`、`Reminders` 等通用 List，让“续费”“月底处理”等泛化标题获得更准确的图标。
- 普通动作备注只在 List 完全没有命中时使用，不会把明确的音乐、物流、财务等 List 改成其他类别。标题、List 与备注分别匹配，不会跨字段拼接词语造成误判。
- 提醒快照只缓存白名单内的图标提示及 `strong`／`ordinary` 置信度，不缓存备注正文、命中词或任何文本片段。旧快照继续兼容；更新后运行一次主脚本实时同步，即可为旧提示补充置信度。

## 更新与使用

本次不迁移或重写事项，不改变小、中、大及锁屏组件的尺寸、字号、间距、容量、点击区域、系统语言逻辑或通知安排。更新后运行一次主脚本完成实时同步，再等待 WidgetKit 刷新即可。

## 明确限制

- 图标推断完全在本机按可审计词库完成，并非联网 AI；未覆盖的新品牌或高度含糊的内容仍会安全回退到 List 分类或任务图标。
- WidgetKit 决定桌面组件的实际刷新时机；短时间显示旧图标不表示标题或数据丢失。

回归范围与真机验收步骤见 `docs/QA-v2.5.5.md`。
