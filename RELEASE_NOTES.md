# 到期管家 v2.5.8

## 本次检测与修复

- 修复组件中“点按重试”误入操作历史的问题：同步重试打开主界面并触发读取，完成操作警告仍打开操作详情。
- 修复同步乱序覆盖新缓存，以及完成事项或关闭同步后在途旧查询重新填回缓存的问题。正常的多组件并发优先使用相同范围的新结果，不会只因并发显示错误提示。
- 组件等待 Apple 提醒事项后重新读取手动事项与显示设置；若提醒列表改变，最多重试一次，持续变化时保留手动事项并提示重新同步。
- 离线缓存按当前日期范围过滤，截止边界的全天事项保持可见；并发的长短查询范围不会被当成等价结果。
- 列表保存期间禁止重复提交，快速连续勾选使用最新状态；旧授权或同步回调不能覆盖较新的界面操作。
- 无效名称、无效日期及超出支持年份的周期推进在写入前被拒绝，保留原事项和完成记录。年份 0001–0099 的计算与显示不再偏移到 1900 年代。

## 更新方法

使用原固定安装链接或脚本内“检查并更新版本”升级到 2.5.8，然后运行一次主脚本。无需重新添加事项、小组件或导入数据。缓存新增查询元数据兼容旧缓存，事项数据格式仍为 schema 3。

## 验证边界

自动化验证覆盖核心流程、乱序请求、跨模块运行实例、缓存失败、列表变更、日期和通知。共享 Storage 没有跨进程原子比较写入能力；本次使用失效标记、查询时间与写前复查缩小竞争窗口，不具备数据库事务保证。iPhone 上实际图标刷新、布局、跳转、动画和通知送达仍需真机验收。

检测记录与验收步骤见 `docs/QA-v2.5.8.md`。
