# 到期管家 v2.5.6

## 本次改进

- 小、中、大组件中的 Apple 提醒事项采用分离点击区：左侧类别图标继续只负责完成事项；标题、备注／日期等内容区域打开系统「提醒事项」中的对应事项。小号顶部日期／类别与大号顶部摘要也使用当前事项的相同链接。
- 小号底部下一事项预告会打开预告对应的提醒，不会复用当前事项的目标。手动事项的文字内容仍打开到期管家编辑页。
- 裸 UUID 和可确认的旧 URL 包装会统一生成 `x-apple-reminderkit://REMCDReminder/{UUID}`；本实现不附加 `/details`。无法提取有效 UUID 时会安全回退到通用 `x-apple-reminderkit://`，只打开系统「提醒事项」。

## 更新与使用

本次不迁移或重写事项，不改变组件尺寸、字号、间距、容量、排序、图标推断、系统语言或通知安排。更新后运行一次主脚本并等待 WidgetKit 刷新；点击左侧图标验证完成操作，点击同一行的文字与日期验证详情跳转。

## 明确限制

- `x-apple-reminderkit` 是 Apple 未公开的 URL Scheme，不属于可承诺长期兼容的公开 API；必须在 iPhone 真机和实际 iOS 版本上验收，未来系统更新后也需要复测。
- 跳转地址特意不添加 `/details`；该后缀在部分 iOS 版本上有不兼容报告。即使 UUID 格式有效，系统仍可能因版本、权限或内部实现变化而无法定位；无效 ID 的通用后备只负责打开「提醒事项」，不保证定位具体事项。
- 该私有 Scheme 仅用于内容区导航，不参与左侧图标的完成逻辑。WidgetKit 仍决定交互后的实际刷新与队列补位时机。

回归范围与真机验收步骤见 `docs/QA-v2.5.6.md`。
