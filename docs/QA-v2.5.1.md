# v2.5.1 回归与验收

## 自动化验证

- 本地结果：225 项测试全部通过；香港、洛杉矶和 UTC 时区分别运行均通过。类型检查与 `git diff --check` 通过，安装包解压内容与源码一致。
- `bun test tests`：保留原有日期、周期、布局、图标和同步测试，增加通知替换／临界时刻、缓存失败降级／跨时区、原始数据隔离恢复／失败保护、备份往返／版本边界、错误事件确认。
- `tests/startup.test.ts` 执行实际启动函数并替换导航适配器，验证损坏数据先进入恢复页、关闭不写入、恢复后再加载、错误深链和原有详情入口。
- `tsc -p tsconfig.validation.json`：UI 接口仍使用测试声明，不能代替原生验证。
- 对安装包解压并逐文件比对源码；云端再次跑测试和类型检查；发布后下载固定 latest 资源并核对版本及 SHA-256。

## iPhone 待验收（不能用模拟测试声称完成）

请使用临时事项，不要拿真实账单或银行提醒做完成／删除测试。

1. 更新显示 2.5.1，旧事项、列表筛选、小中大组件的位置、图标与时间槽位不变。
2. Apple 提醒完成后核对实际系统状态；如发生缓存异常，组件「检查操作」应直达结果页。清除提示不改变完成状态，旧页面不得清除同文案新事件。
3. 香港→洛杉矶切时区并使用缓存：定时事项的标题日期与时分一致，全天日期不变；只读或缓存事项仍不能从组件完成。
4. 完成测试事项→导出备份→原样恢复→撤销，原本匹配的记录仍可撤销；之后编辑或再次完成的记录仍拒绝覆盖。
5. 损坏主数据／快照索引只在隔离测试脚本的模拟 Storage 中验证，不主动损坏用户真实数据。验证独立恢复入口、原文隔离、写入失败不覆盖、更高格式禁止覆盖，以及原始档案可导出。
6. 已排入 10 条测试通知后切换语言，普通组件运行不应先全删再只补 3 条。切时区后已存在的时刻变更应优先修复；无宿主余量时明确提示待修正。
7. 临近通知时刻触发慢查询，不应将过去时刻显示为已安排的未来通知；系统是否显示、何时显示仍以实际通知检查为准。
8. 拒绝通知权限、恢复权限、关闭通知及删除测试事项，检查状态说明和过期／重复请求清理。别的脚本通知不得被删除。

已知系统边界继续适用：滚动通知需后续运行、WidgetKit 刷新由系统决定、无私有 Reminders 动画、共享 Storage 无跨进程原子事务。替换采用先新后旧，极短的过渡或进程中断可能留下待下次去重的副本。
